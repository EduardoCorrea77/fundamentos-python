def inverter_lista(lista):
    lista_invertida = list(reversed(lista))

    print(f"Lista invertida: {lista_invertida}")

    return lista_invertida

nomes = ["Eduardo", "Felipe", "Kael", "Murillo"]

inverter_lista(nomes)